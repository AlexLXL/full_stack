function test(n) {
    let dp = []
    for(let y = 0; y < (n / 2) << 0; y++) {
        dp[y] = []
        for(let x = 0; x < n; x++) {
            if(y === 0) {
                dp[y][x] = x * 1
            }
            if(x === 0) {

            }
        }
    }
};